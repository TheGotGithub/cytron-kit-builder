# ── Web Framework ─────────────────────────────────────────
Flask==3.1.0
flask-cors==6.0.5

# ── AI / Embedding ─────────────────────────────────────────
# RPi CM5 (ARM64): torch wheel ขนาดใหญ่ ใช้เวลา build นาน
# ถ้า pip install ช้า ให้ใช้: pip install torch --index-url https://download.pytorch.org/whl/cpu
torch==2.12.1
sentence-transformers==5.6.0

# ── Data ───────────────────────────────────────────────────
numpy==1.26.4
scikit-learn==1.9.0
