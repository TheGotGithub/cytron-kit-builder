# Copy ไฟล์นี้เป็น .env แล้วแก้ค่าตามต้องการ
# cp .env.example .env

# Port ที่แอปจะรัน (default: 5050)
PORT=5050

# Host (0.0.0.0 = รับทุก interface, ใช้บน RPi เพื่อเข้าจาก network อื่น)
HOST=0.0.0.0
